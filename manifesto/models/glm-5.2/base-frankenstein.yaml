# Shared settings for direct-PCP/NIXL prefill with DSpark decode.
#
# Keep this file abstract (base-* files are skipped by validation). Concrete
# deployments own all topology and capacity values.
extends: base.yaml
model:
  id: zai-org/GLM-5.2-FP8
  label: GLM-5.2-FP8
  served_name: zai-org/GLM-5.2-FP8
vars:
  dspark: &dspark
    method: dspark
    model: RedHatAI/GLM-5.2-speculator.dspark
    num_speculative_tokens: 7
    draft_sample_method: probabilistic
    kv_cache_dtype: fp8
roles:
  prefill:
    env:
      VLLM_WORKER_MULTIPROC_METHOD: spawn
      NVSHMEM_REMOTE_TRANSPORT: ibgda
      NVSHMEM_IB_ENABLE_IBGDA: "true"
      NVSHMEM_HCA_PREFIX: ibp
      NVSHMEM_BOOTSTRAP_UID_SOCK_IFNAME: eth0
      NVIDIA_GDRCOPY: enabled
    vllm:
      moe_backend: deep_gemm
      all2all_backend: deepep_high_throughput
  decode:
    dp_load_balancing: external
    env:
      VLLM_WORKER_MULTIPROC_METHOD: spawn
      NVSHMEM_QP_DEPTH: "1026"
    vllm:
      moe_backend: deep_gemm
      all2all_backend: deepep_low_latency
      speculative_config: *dspark
