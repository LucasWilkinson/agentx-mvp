# Benchmarkable fallback while overlapping PCP8 x DCP8 query exchange is fixed.
# Prefill uses the supported replicated PCP topology; decode remains DP8 + EP.
extends: base-frankenstein.yaml
release: glm52-p1-pcp8ep-d1-dp8ep-dspark
roles:
  prefill:
    parallelism: {gpus: 8, tp: 1, pcp: 8, dp: 1, ep: true}
    env:
      MANIFESTO_VLLM_ENV: /workspace/vdptest/frankenstein-prefiller-lucas-dcp
      PYTHONPATH: /workspace/vdptest/frankenstein-prefiller-lucas-dcp
      VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS: "0"
    vllm:
      # DCP1 has 9.04 GiB of KV cache (~179k-token ceiling); match the decoder.
      max_model_len: 114688
      gpu_memory_utilization: 0.90
      max_num_batched_tokens: 32768
      max_num_seqs: 16
      enforce_eager: true
      kernel_config:
        enable_flashinfer_autotune: false
  decode:
    parallelism: {tp: 1, dp: 8, ep: true}
    env:
      MANIFESTO_VLLM_ENV: /workspace/vdptest/frankenstein-prefiller-lucas-dcp
      PYTHONPATH: /workspace/vdptest/frankenstein-prefiller-lucas-dcp
      VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS: "0"
    vllm:
      max_model_len: 114688
      gpu_memory_utilization: 0.94
      max_num_batched_tokens: 512
      max_num_seqs: 64
