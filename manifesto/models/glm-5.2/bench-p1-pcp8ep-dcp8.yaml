# PCP8 x DCP8 x TP1 + EP prefill-only on nm/glm-53-blog/prefiller, no profiler (the API server wedges
# after /stop_profile with very large traces). For correctness and throughput checks.
extends: prof-p1-pcp8ep-dcp8.yaml
release: glm52-bench-p1-pcp8ep-dcp8
roles:
  prefill:
    env:
      VLLM_PCP_DCP_DEBUG: "0"
    vllm_raw_args:
      - -cc.cudagraph_mode=NONE
