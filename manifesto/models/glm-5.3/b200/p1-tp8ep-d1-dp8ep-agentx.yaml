# No-MTP diagnostic variant. Keep separate from the canonical MTP3 curve.
extends: p1-tp8ep-d1-dp8ep-mtp-agentx.yaml
roles:
  prefill:
    vllm:
      speculative_config: $delete
  decode:
    vllm:
      speculative_config: $delete
